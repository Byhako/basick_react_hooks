import { makeStyles } from '@material-ui/styles';

export default makeStyles({
  container: {
    padding: 5,
    width: '70%',
  },
  poster: {
    width: 200,
    minWidth: 200,
    marginTop: 50,
    marginBottom: 50
  },
  loader: {
    display: 'flex',
    flexDirection: 'column',
    alignItems: 'center',
  }
})